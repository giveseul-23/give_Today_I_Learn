<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BBS">

	<!-- 전체 게시글 수 조회  -->
	<select id="totalCount" resultType="int">
		SELECT COUNT(*) AS CNT FROM BBS_T
	</select>
	
	<!-- 
		페이지에 표시할 데이터 조회  
		parameterType="Map" : begin, end 값 전달 용도
		key-value 형태의 Map 사용 바인드변수명 key가 됨
	-->
	<select id="list" resultType="com.bc.mybatis.BBSVO"
		parameterType="Map">
		SELECT *
			FROM(SELECT ROWNUM AS R_NUM, B.*
				FROM(SELECT * 
						FROM BBS_T
						ORDER BY B_IDX DESC
				) B
			)
		WHERE R_NUM BETWEEN #{begin} AND #{end}
	</select>
	
	<!-- 게시글 하나 조회(B_IDX 값으로) -->
	<select id="one" parameterType="String" resultType="BBSVO">
	SELECT B_IDX, SUBJECT, WRITER, CONTENT
	    , FILE_NAME, ORI_NAME, PWD, WRITE_DATE
	    , IP, HIT
		FROM BBS_T
		WHERE B_IDX = #{b_idx}
	</select>
	
	
	<!-- 게시글 입력처리  -->
	<insert id="insert" parameterType="BBSVO" >
		INSERT INTO BBS_T
			(b_idx, subject, writer
			, content, file_name, ori_name
			, pwd, write_date, ip, hit)
		VALUES (BBS_T_SEQ.NEXTVAL, #{subject}, #{writer}
			, #{content}, #{file_name}, #{ori_name}
			, #{pwd}, SYSDATE, #{ip}, 0)
	</insert>
	
	<!-- 게시글 수정처리 -->
	<update id="update">
		
	</update>
	
	<!-- 게시글 삭제처리 -->
	<!-- 게시글 내 댓글있는 확인 -->
	<select id="checkComment" parameterType="String" resultType="com.bc.mybatis.CommVO">
		SELECT * FROM COMMENT_T
		WHERE B_IDX = #{b_idx};
	</select>
	<!-- 게시글 삭제 -->
	<delete id="delete" parameterType="String">
		DELETE FROM BBS_T
		WHERE B_IDX = #{b_idx}
	</delete>
	
	<!-- 조회수 1증가 처리 -->
	<update id="hit" parameterType="String">
		UPDATE BBS_T
		SET HIT = HIT + 1
		WHERE B_IDX = #{b_idx}
	</update>
	
	<!-- =========댓글처리========== -->
	<!-- 게시글에 딸린 댓글 조회  -->
	<select id="commList" parameterType="String" resultType="com.bc.mybatis.CommVO">
		SELECT *
		 FROM COMMENT_T
		 WHERE B_IDX = #{b_idx}
		 ORDER BY C_IDX DESC
	</select>
	
	<!-- 댓글입력 -->
	<insert id="c_insert" parameterType="com.bc.mybatis.CommVO">
		INSERT INTO COMMENT_T(C_IDX, WRITER, CONTENT, PWD, WRITE_DATE, IP, B_IDX)
		VALUES(COMMENT_T_SEQ.NEXTVAL, #{writer}, #{content}, #{pwd}, SYSDATE, #{ip}, #{b_idx})
	</insert>
	
	<!-- 댓글삭제 -->
	<delete id="c_delete" parameterType="hashmap">
		DELETE FROM COMMENT_T
		WHERE B_IDX = #{b_idx} AND C_IDX = #{c_idx}
	</delete>
	
</mapper>









